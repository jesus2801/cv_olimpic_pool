Detección de Personas con YOLO
Para la detección de nadadores se empleará el modelo de detección de objetos YOLO (You Only Look Once), 
un algoritmo de visión por computadora que permite identificar objetos en imágenes en tiempo real.
